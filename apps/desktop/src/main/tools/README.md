# main/tools

Tool registry wiring. Loads built-in tools from `@opencodex/tools`, MCP-discovered tools from `@opencodex/mcp-client`, and plugin-contributed tools via the plugin host. Enforces permission tiers and dispatches approval prompts.
