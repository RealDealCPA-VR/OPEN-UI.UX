# main/providers

Provider registry wiring. Loads built-in provider adapters from `@opencodex/providers` plus any plugin-contributed providers. Reads API keys from `keytar` and validates configs against `LLMProvider` capabilities.
