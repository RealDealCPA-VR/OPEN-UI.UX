# main/rag

Codebase indexing and retrieval. Tree-sitter AST chunker, embedding via the configured embedding provider, LanceDB vector store, SQLite FTS5 keyword search, hybrid retrieval with RRF. File watcher drives incremental reindex.
