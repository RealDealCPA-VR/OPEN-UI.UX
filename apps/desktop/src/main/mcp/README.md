# main/mcp

MCP server lifecycle management: spawn/connect to configured servers (stdio, SSE, HTTP), surface their tools, resources, and prompts through the unified tool registry, handle reconnect and auth.
