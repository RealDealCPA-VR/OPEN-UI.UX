# hello-world

Minimal reference plugin for the OpenCodex plugin SDK. Demonstrates:

- the `opencodex.plugin.json` manifest format
- registering a tool via the host API
- using Zod for input validation

To install in development: point OpenCodex at this directory via Settings → Plugins → Install from folder.
